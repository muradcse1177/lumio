# dependencies
node_modules/

# build output
dist/
build/

# SECRETS — never commit (license signing key)
*.pem

# packaged installers (delivered via GitHub Releases, not the repo)
installers/

# misc
*.log
.DS_Store
Thumbs.db
